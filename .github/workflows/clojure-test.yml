name: Clojure CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    # Run tests every two months on the 1st day at 09:00 UTC
    # This runs on January 1st, March 1st, May 1st, July 1st, September 1st, November 1st
    - cron: '0 9 1 1,3,5,7,9,11 *'

env:
  # Increment this to invalidate all caches
  ACTIONS_CACHE_VERSION: 0

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ env.ACTIONS_CACHE_VERSION }}-m2-${{ hashFiles('deps.edn', 'bb.edn') }}
      - name: Install Clojure CLI
        run: |
          curl -O https://download.clojure.org/install/linux-install.sh
          chmod +x linux-install.sh
          sudo ./linux-install.sh
      - name: Check dependabot is in sync with deps.edn
        run: ./script/check-dependabot
      - name: Install Babashka
        run: |
          curl -sLO https://raw.githubusercontent.com/babashka/babashka/master/install
          chmod +x install
          sudo ./install
      - name: Run tests with Clojure
        run: ./script/test
      - name: Run tests with Babashka
        run: ./script/test-bb
      - name: Run doc block tests
        run: ./script/test-doc-blocks
